;(function() {

    var global = this;

    var Channel = function Channel(options) {
        Sphere.Core.Base.call(this, options);
    };

    Sphere.Module.Channel = Channel;

}.call(this || window));
